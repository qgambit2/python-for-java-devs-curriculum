"""Lesson 2 index — collections (list, dict, tuple, set) & slicing."""

from _lesson_runner import run_lesson_index

LESSONS = ["01_collections.py", "02_slicing.py"]

if __name__ == "__main__":
    run_lesson_index(2, LESSONS)
